# ip-rules.conf (managed by nodeapi)
# lines will be like:
#MODE=blacklist
#
#allow 198.51.100.10;
#deny 203.0.113.0/24;
#
# Node writes rules below this header.
# Default: blacklist mode (allow by default; denies only those listed)
# To set whitelist mode, include: MODE=whitelist
